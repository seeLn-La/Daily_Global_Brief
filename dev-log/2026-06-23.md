# 2026-06-23 开发日志

## 已完成

- 重新整理 `scripts/config.py` 的 RSS 源清单
- 将科技、商业、AI 源按主来源和补位来源分层
- 优先保留更稳定、更新更快、主题更纯的媒体源
- 为后续“先抓 20-30 篇候选，再筛出 10 篇成品”打基础
- 调整 `scripts/aggregator.py`，加入候选池和最终池两层筛选
- 候选池按来源限量保留，避免单一来源占满板块
- 最终输出仍保持每板块 10 篇，兼容现有页面和推送逻辑
- 在 `scripts/aggregator.py` 增加板块相关性打分
- 相关性打分同时参考标题关键词、来源可信度和发布时间
- 最终展示会优先保留更像本板块的文章，减少跑偏内容
- 在 `scripts/aggregator.py` 增加三档补位策略：强相关、弱相关、数量补位
- 每条最终新闻会记录 `selection_tier`、`selection_reason`、`selection_score`
- 更新 `docs/tech-spec.md` 的 JSON 示例，和新输出结构保持一致
- 试跑 `scripts/aggregator.py`，确认代码路径可执行
- 当前沙盒环境无法解析外网 RSS 域名，实际抓取结果为空
- 在可联网环境试跑，确认候选池和最终池都能产出
- 真实抓取下科技/商业/AI 分别能凑到 18 / 20 / 14 篇候选，最终都能补足到 10 篇
- 发现部分 AI 源存在 403、超时或解析异常，已从主配置中移除
- 翻译超时从 8 秒收短到 3 秒，降低整轮等待时间
- 将 `scripts/fetcher.py` 改成并发抓取
- 增加 `data/source_health.json`，记录连续失败的 RSS 源
- 连续失败的源会被临时跳过，避免每天重复拖慢整轮
- 并发任务异常会单独捕获，不影响其他源
- 更新 `docs/tech-spec.md`，写入新的抓取容错策略
- 从配置中移除持续不稳的科技和 AI 来源：`geekpark`、`tmtpost`、`zhidx`
- `source_health.json` 会自动清理已移除的旧源记录
- 当前配置下抓取仍可稳定拿到 554 篇去重后文章
- 更新 `docs/execution-plan.md`，补充日常维护说明

## 待办

- 观察实际抓取结果，必要时继续调关键词和分数阈值
- 在可联网环境或 GitHub Actions 中复测，确认最终新闻数量与主题分布
- 继续观察每日实际产出，必要时再微调科技/AI 的候选源
- 继续观察 `source_health.json`，必要时收缩更多不稳定源
- 当前抓取量仍充足，后续优先观察是否还有新的长期不稳源

## 决策记录

- 采用“候选池先放大，再筛选收窄”的方式，而不是只靠增加 RSS 源数量
- 源列表改为分层管理，方便后续继续替换低质量来源
- 最终展示加入补位标记，便于后续排查为什么某天内容偏少或偏弱
- 真实环境下，AI 源的稳定性明显弱于科技和商业，需要更谨慎地保留来源
- 并发抓取 + 健康记录，比单纯扩大源列表更适合这个日更场景
- 清理旧源记录有助于让健康文件保持和配置同步，减少噪音
- 维护说明里需要明确“系统会自动跳过坏源”，减少后续误解
